填完 Excel 后，请在聊天里逐条回复（可简写）：

1. T 的含义：每行时间是「必须在该时刻点开始」的单点，还是「某时间段内完成」的窗口？若是窗口，提醒以窗口开始还是结束为基准？

2. 「完成」认定：是否仅由值班员在界面点「已完成」？是否要对接外部系统才算完成？

3. 检查单是否按日 / 按班次 / 按席位变化？值班长是否可改当日条目？

4. 提醒出口：仅工作台内（Toast / 红点 / 声音），还是也要丰声等？关页时是否接受「打开工作台时补算 missed」+ 可选系统通知？

5. 超时后：是否允许补做并标记「已补做」，但仍保留「未按时完成」存档供质检？
